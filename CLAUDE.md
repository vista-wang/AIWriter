# CLAUDE.md — AIWriter

Mirrors `AGENT.md`.

Client-only Vite + React novel writer. Writing/revise vs ask prompts are separate. Validate URLs, sanitize storage, abort on unmount. See `SECURITY.md` for public deployment notes.

Keep in sync with `AGENT.md`.
