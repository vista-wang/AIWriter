# Copy to .env.local for local overrides. Do not commit real secrets.

# --- Auth (Supabase) — reserved, currently disabled ---
# Set VITE_AUTH_ENABLED=true and fill URL + anon/publishable key to enable later.
VITE_AUTH_ENABLED=false
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
